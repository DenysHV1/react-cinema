export const searchedInfoSelector = (state) => state.search.searched;
export const searchedErrorSelector = (state) => state.search.error;
export const searchedNameSelector = (state) => state.search.name;